manifestVersion: subsquid.io/v0.1
name: showcase01-all-usdc-transfers
version: 1
description: |-
  The very first evm squid from manifest
build:
deploy:
  addons:
    postgres:
    rpc:
      - gnosis.http # RPC proxy URL will be supplied via the RPC_GNOSIS_HTTP env variable
  processor:
    cmd: ["sqd", "process:prod"]
  api:
    cmd: ["sqd", "serve:prod"]
